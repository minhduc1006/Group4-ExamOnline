import AddEmailPage from "@/app/email-service/add-email/AddEmailPage";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Email",
};

const AddEmail = () => {
  
  return (
    <AddEmailPage/>
  );
};

export default AddEmail;

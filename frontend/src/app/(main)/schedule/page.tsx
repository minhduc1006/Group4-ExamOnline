import SchedulePage from "@/components/longnt/schedule/SchedulePage";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Schedule",
};

export default function Schedule() {
  return <SchedulePage />;
}

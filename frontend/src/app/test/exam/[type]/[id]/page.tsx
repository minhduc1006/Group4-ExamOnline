import ExamPage from "../../../../../components/ExamPage";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Do Exam",
};

const Exam = () => {
  return <ExamPage />;
};

export default Exam;
